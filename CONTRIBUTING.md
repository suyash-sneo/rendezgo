## Contributing

Thanks for helping make rendez better!

1. Run `gofmt` on touched files.
2. Add tests for new behavior.
3. Run `go test ./...` (and `go test -race ./...` locally if possible).
4. Keep changes small and focused; include rationale in PR description.

Bug reports and design feedback are welcome via issues or PRs.
